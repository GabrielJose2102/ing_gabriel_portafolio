---
---
<section class="relative pt-20 pb-16 lg:pt-32 lg:pb-24">
  <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
    <div>
      <!-- Badge de disponibilidad -->
      <div class="inline-flex items-center space-x-2 rounded-full bg-emerald-500/10 px-3 py-1 text-sm font-medium text-emerald-400 ring-1 ring-inset ring-emerald-500/20 mb-6">
        <span class="relative flex h-2 w-2">
          <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-emerald-400 opacity-75"></span>
          <span class="relative inline-flex rounded-full h-2 w-2 bg-emerald-500"></span>
        </span>
        <span>Consultoría Odoo v17/v18 & Software Senior</span>
      </div>

      <h1 class="text-4xl font-extrabold tracking-tight text-white sm:text-6xl mb-6 leading-tight">
        Arquitectura de Sistemas <br/>
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-emerald-400 to-emerald-600">
          con Precisión Financiera
        </span>
      </h1>

      <p class="text-lg leading-8 text-slate-400 max-w-2xl mb-10">
        Ingeniero de Software y Especialista Odoo. Optimizo flujos críticos de negocio mediante arquitecturas limpias, lógica fiscal SENIAT y soluciones de alto rendimiento potenciadas por IA.
      </p>

      <div class="flex flex-wrap gap-4">
        <a 
          href="#proyectos" 
          class="rounded-lg bg-emerald-600 px-6 py-3 text-sm font-semibold text-white shadow-sm hover:bg-emerald-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-emerald-600 transition-all"
        >
          Explorar Proyectos
        </a>
        <a 
          href="mailto:gabrieltorrealba09@gmail.com" 
          class="rounded-lg bg-slate-800 px-6 py-3 text-sm font-semibold text-white shadow-sm ring-1 ring-inset ring-slate-700 hover:bg-slate-700 transition-all"
        >
          Contactar
        </a>
      </div>

      <!-- Quick Tech Stack -->
      <div class="mt-12 flex flex-wrap gap-6 items-center opacity-50 grayscale hover:grayscale-0 transition-all duration-500">
        <span class="text-xs font-bold tracking-widest text-slate-500 uppercase">TECH STACK:</span>
        <div class="flex gap-4 text-sm font-medium text-slate-300">
          <span>Python</span>
          <span>Odoo</span>
          <span>PostgreSQL</span>
          <span>Flutter</span>
          <span>Clean Architecture</span>
        </div>
      </div>
    </div>

    <!-- Visual: Minimalist Code Block -->
    <div class="relative hidden lg:block">
      <div class="relative rounded-2xl border border-slate-800 bg-slate-900/50 p-6 shadow-2xl backdrop-blur-sm">
        <div class="flex items-center space-x-2 border-b border-slate-800 pb-4 mb-4">
          <div class="h-3 w-3 rounded-full bg-red-500/50"></div>
          <div class="h-3 w-3 rounded-full bg-yellow-500/50"></div>
          <div class="h-3 w-3 rounded-full bg-green-500/50"></div>
          <span class="text-xs text-slate-500 font-mono ml-4 tracking-widest uppercase">fiscal_logic.py</span>
        </div>
        <pre class="text-sm font-mono text-emerald-400/90 leading-relaxed overflow-hidden">
<code><span class="text-slate-500"># Localización Venezuela SENIAT</span>
class FiscalLocalization:
    def __init__(self, company):
        self.tax_id = company.vat
        
    def calculate_retention(self, invoice):
        <span class="text-emerald-200"># Lógica de cálculo multimoneda</span>
        rate = BCV_API.get_rate()
        amount_ves = invoice.amount * rate
        
        return self.process_lote(amount_ves)</code>
        </pre>
      </div>
      <!-- Efecto de brillo detrás del código -->
      <div class="absolute -inset-1 rounded-2xl bg-emerald-500/10 blur-2xl -z-10"></div>
    </div>
  </div>
</section>